
import { betStatus } from 'nfl-bet-front/helpers/bet-status';
import { module, test } from 'qunit';

module('Unit | Helper | bet status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = betStatus([42]);
  assert.ok(result);
});

